@tailwind base;
@tailwind components;
@tailwind utilities;


.main-text {
  @apply font-serif text-black;
}

.heading-1 {
  @apply font-serif text-2xl text-black;
}

.drop-down {
  @apply after:contents
}

.wrapper {
  @apply w-full max-w-7xl flex justify-between px-16 py-5;
}