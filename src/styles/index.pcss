@tailwind base;
@tailwind components;
@tailwind utilities;


.main-text {
  @apply font-serif text-black mb-6 mx-9 text-3xl;
  line-height: 50px;
  letter-spacing: 0.1rem;
}

.heading-1 {
  @apply font-serif text-2xl text-black;
}

.wrapper {
  @apply w-full max-w-7xl flex justify-between px-16 py-5;
}
